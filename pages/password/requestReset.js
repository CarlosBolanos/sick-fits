import React from "react";
import RequestResetPasswordForm from "../../components/RequestResetPasswordForm";

const RequestReset = ({ query }) => {
  return (
    <div>
      <RequestResetPasswordForm />
    </div>
  );
};

export default RequestReset;
