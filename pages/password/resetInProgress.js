import React from "react";

const ResetInProgress = ({ query }) => {
  return (
    <div className="p-10">
      We received your request to reset your password.
      <br />
      You should be receiving an email with instructions in a couple of minutes.
    </div>
  );
};

export default ResetInProgress;
