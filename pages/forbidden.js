import React from "react";

const Forbidden = ({ query }) => {
  return (
    <div className="p-10">
      The part of the site you are trying to access is forbidden.
    </div>
  );
};

export default Forbidden;
